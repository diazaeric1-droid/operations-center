[pytest]
# Product tests only. The vendored components under apps/ ship their own test
# suites that expect to run from their own repo roots (top-level `src` package);
# they are exercised upstream, not here.
testpaths = tests
