# Operations Center — union of the absorbed components' runtime deps:
#   pe-pipeline (orchestrator), daily-production-digest, deferment-iq,
#   esp-failure-risk-agent (scoring), afe-copilot (economics).
# One environment runs the whole console in-process (see core.py).
# python-docx is intentionally OMITTED: the AFE stage renders markdown only
# (afe.handoff imports cost_db/economics/models/risk_register/tracker — not
# docx_builder), matching pe-pipeline's deployment.
streamlit>=1.50,<2
plotly>=5.18.0,<7
pandas>=2.2.0,<3
numpy>=1.26.0,<3
scikit-learn>=1.6.0,<1.10
xgboost>=2.0.0,<3
joblib>=1.3.0
python-dotenv>=1.0.0
pyyaml>=6.0
anthropic>=0.40.0
rich>=13.7.0
